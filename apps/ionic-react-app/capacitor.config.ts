import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicReactApp',
  webDir: '../../dist/apps/ionic-react-app',
  bundledWebRuntime: false,
};

export default config;
